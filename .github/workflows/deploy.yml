name: Deploy Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Executar Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            # Entrar na pasta
            cd /root/FinTropy-api

            # Baixar código novo
            echo "📥 Baixando atualizações..."
            git pull origin main

            # Construir imagem
            echo "🏗️ Construindo imagem..."
            docker build -t fintropy-api:latest .

            # Atualizar Swarm
            echo "🔄 Atualizando serviço..."
            docker service update --image fintropy-api:latest --force fintropy-api_api

            # Limpar lixo
            docker image prune -f
